function JoinScreen({start}){
    return (
        <div className="join-screen">
            <h2>Join Quiz</h2>
            <p>Make quizzes that get you leads, engage your customers, and show off your brand.</p>
            <button onClick={start}>Start</button>
        </div>
    );
}

export default JoinScreen;